# Jury Report — {{ID}} {{TITLE}} — Round {{ROUND}}

> Independent, unbiased Awwwards-style evaluation of the **rendered** UI. The jury never saw the
> makers' reasoning — only the screenshots below and the fixed rubric. Owned by `aidlc-ux-jury`.
> Gate: composite must be **≥ {{THRESHOLD}}/10**. A 9 is rare and must be earned.

## Evidence

Rendered at `{{URL}}` · viewport {{WxH}} · captured {{UTC}}.
Screenshots reviewed (paths): {{list}}

## Scorecard

Each dimension scored /10 with **concrete visual evidence** (what in the screenshot earns/loses it).
Composite = weighted sum, rounded to one decimal.

| Dimension | Weight | Score /10 | Evidence & specific defects |
|---|---|---|---|
| Design / aesthetics (hierarchy, type, color, spacing) | 25% | | |
| Usability / UX clarity | 20% | | |
| Creativity / originality | 15% | | |
| Motion & interaction craft | 20% | | |
| Consistency / uniformity (token adherence across screens) | 12% | | |
| Polish & states (pixel precision, hover/focus/empty/error, a11y contrast) | 8% | | |
| **Composite** | 100% | **{{SCORE}}** | |

## Verdict

`PASS (≥{{THRESHOLD}})` or `ITERATE (<{{THRESHOLD}})`.

## Required fixes (only if ITERATE)

Ordered, specific, each addressed to an agent (`design-system` / `motion` / `implementer`) with
the exact defect and what "fixed" looks like. No vague notes — the makers must be able to act
without guessing.

1. …

## What's working (keep)

Brief — so the next round doesn't regress the strengths.
