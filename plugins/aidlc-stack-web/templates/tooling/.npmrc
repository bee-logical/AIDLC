; Bee-Logical AIDLC web-stack npm defaults
; Fail install if the engine (Node version) doesn't match package.json "engines".
engine-strict=true
; Pin exact versions for direct deps — deterministic, SaaS-grade installs (lockfile still authoritative).
save-exact=true
